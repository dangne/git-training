#include <bits/stdc++.h>
using namespace std;

void Cos_Functions( float X ){
    if ( cos(X)<(-1) || cos(X)>1 ) cout<<"Input khong hop le\n";
        else cout<<cos(X);
}

int main(){
    // Hello this is Dang
    return 0;
}
